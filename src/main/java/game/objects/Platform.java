package game.objects;

import javafx.scene.image.ImageView;

public class Platform extends GameObject{
    public Platform(double x, double y, ImageView imageView) {
        super(x, y, imageView);
    }
}
